import { describe, it } from 'vitest';
import { firstpointCryptoPlugin } from '../index';

describe('firstpointCryptoPlugin', () => {
    it('should always pass', () => {
        expect(true).toBe(true);
    });
});